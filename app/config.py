"""Global state and configuration management.

Migration target for these ``main.py`` functions and globals:
    DATA_FILE_PATH, DATA_FILE_TYPE, CONFIG_PATH, CONFIG_DATA, AUTO_POSITIONS
    set_data_file()
    set_config_file()
    load_config()
    set_config()
"""
