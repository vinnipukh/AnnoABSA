"""Route handlers for the FastAPI application.

Submodules:
    nlp         — NLP Helper Toolbar (lexicon, sentiment, morphology, embedding)
    settings    — GET /settings, PATCH /settings
    reviews     — GET /data/{idx}, POST /review/{idx}/save, POST /agent/chat
    ai          — GET /ai_prediction/{idx}
    upload      — POST /upload-data
"""
