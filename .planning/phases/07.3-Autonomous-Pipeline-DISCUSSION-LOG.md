# Phase 7.3: Autonomous Annotation Pipeline — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-14
**Phase:** 7.3-Autonomous-Pipeline
**Areas discussed:** Template actions, keyboard shortcut, auto-suggest banner, addTriplet signature, predictions endpoint

---

## AUTOPILOT-01 — Template actions

The `DEFAULT_CHAT_TEMPLATE` already has 11 actions with `[[action:...]]` directives. Decision: add the 3 missing actions (`selectTriplet`, `addTriplet`, `annotateAll`) to the prompt. No full rewrite.

## AUTOPILOT-02 — Keyboard shortcut

Reuse existing `keydown` handler pattern at App.tsx:247. Wire `Ctrl+Shift+L` to toggle the Active Learning suggestions panel.

## AUTOPILOT-03 — Auto-suggest banner

| Option | Selected |
|--------|----------|
| Inline banner above review header + toast for dismiss | ✓ |
| Toast notification only | |
| Both — banner + toast for dismiss | Actually selected |

**User's choice:** Both — inline banner above review header with dismiss button, plus toast for secondary notification.

## AUTOPILOT-04 — selectTriplet in prompt

Already exists in AppActions. Just add to template. No backend changes needed.

## AUTOPILOT-05 — addTriplet signature

| Option | Selected |
|--------|----------|
| Simple 3-arg: addTriplet(term, category, polarity) — wrapper over addManualTriplet | ✓ |
| Use existing addManualTriplet(triplet) — agent constructs the object | |

**User's choice:** Simple 3-arg wrapper `addTriplet(term, category, polarity)`.

## AUTOPILOT-06 — Predictions endpoint

| Option | Selected |
|--------|----------|
| New endpoint GET /chat/predictions/{idx} returning chat-formatted text | ✓ |
| Extend GET /learning/predict/{idx} to include chat-formatted field | |

**User's choice:** New dedicated endpoint.

## AUTOPILOT-07 — annotateAll pipeline

User did not decide on confirmation model during discussion. Two options deferred to planning:
- Option A: Execute silently — no user confirmation needed
- Option B: Require confirmation via chat ('Shall I annotate all?')

## Claude's Discretion
- Banner styling and DaisyUI component choice (alert vs custom div)
- Toast duration and positioning
- Confidence threshold for auto-accept in annotateAll pipeline
- Max N reviews for annotateAll pipeline (default: 5)

---

*Phase: 07.3-Autonomous-Pipeline*
*Discussion logged: 2026-07-14*
