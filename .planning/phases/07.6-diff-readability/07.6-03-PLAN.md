---
phase: 07.6-diff-readability
plan: 03
type: execute
wave: 3
depends_on:
  - 07.6-02
files_modified:
  - frontend/src/components/ResolutionPanel.test.tsx
  - frontend/src/components/ResolutionPanel.tsx
autonomous: true
requirements:
  - UX-03
must_haves:
  truths:
    - "All existing frontend tests pass (88 tests, 0 failures)"
    - "ResolutionPanel tests verify the new Birincil Öneri label display for all 3 tiers"
    - "Touch targets in ResolutionPanel remain >= 44x44px (min-h-[44px] on buttons)"
    - "Reduced-motion @media block is preserved in ResolutionPanel.tsx"
    - "Zero emoji in ResolutionPanel.tsx and ResolutionPanel.test.tsx"
  artifacts:
    - path: "frontend/src/components/ResolutionPanel.test.tsx"
      provides: "Updated test assertions matching the restructured layout"
      min_lines: 320
    - path: "frontend/src/components/ResolutionPanel.tsx"
      provides: "Final component with no regressions from plans 01-02"
      contains: "@media (prefers-reduced-motion"
      contains: "min-h-[44px]"
  key_links:
    - from: "frontend/src/components/ResolutionPanel.test.tsx"
      to: "frontend/src/components/ResolutionPanel.tsx"
      via: "Test assertions verify component behavior after plans 01-02 restructuring"
      pattern: "import.*ResolutionPanel"
---

<objective>
Update the ResolutionPanel component tests to match the restructured layout from plans 01-02, verify accessibility constraints (touch targets, reduced-motion), and ensure zero emoji regressions.

Purpose: After repositioning labels (plan 01) and enlarging typography (plan 02), the test assertions for text content and DOM structure need updating. Additionally, accessibility constraints (touch targets >= 44px, reduced-motion media query, emoji-free source) must be verified.
Output: Updated test file with corrected assertions, verified accessibility, and confirmed no regressions.
</objective>

<execution_context>
@$HOME/AppData/Local/hermes/gsd-core/workflows/execute-plan.md
@$HOME/AppData/Local/hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@frontend/src/components/ResolutionPanel.test.tsx
@frontend/src/components/ResolutionPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ResolutionPanel tests for restructured layout</name>
  <files>frontend/src/components/ResolutionPanel.test.tsx</files>
  <read_first>
Read the full files before modifying:
- frontend/src/components/ResolutionPanel.test.tsx — understand current 11 test cases and their assertions
- frontend/src/components/ResolutionPanel.tsx — understand the post-plan-02 final structure (Birincil Öneri with labels, Fark Takibi with diff-below)
  </read_first>
  <action>
Update test assertions in ResolutionPanel.test.tsx to match the restructured layout from plans 01-02. Key changes needed:

**Test: "renders Tier 1 Otomatik Kabul with green header" (lines 80-95):**
- Current assertion: `expect(container.textContent).toContain('Manzara')` and `'servis'` from Fark Takibi (lines 93-94)
- After plan 01: majorityLabel triplets now appear in Birincil Öneri, not Fark Takibi
- Update: verify Birincil Öneri box contains the majorityLabel triplet text. Check that Fark Takibi contains "GT ile eşleşiyor" instead of the triplet list
- Keep: header class check (`.bg-success\\/10`), Otomatik Kabul title check, no deprecated badge check

**Test: "shows Accept and Edit buttons in Tier 1" (lines 97-105):**
- Should still pass without changes (action buttons unaffected by plans 01-02)

**Test: "renders Tier 2 Hızlı Fark with yellow header" (lines 109-132):**
- After plan 01: majorityLabel triplets in Birincil Öneri; comparison grid BEFORE diff text in Fark Takibi
- After plan 02: text sizes are larger (text-xs, text-[10px], text-sm)
- Update: verify Birincil Öneri shows majorityLabel terms; verify Fark Takibi has comparison grid (Çoğunluk, GT (Orijinal)) BEFORE diff text (Çoğunluk positive)
- The text content assertions should still work — Çoğunluk, GT labels remain

**Test: "shows three action buttons in Tier 2" (lines 134-148):**
- Should still pass without changes

**Test: "renders Tier 3 Manuel İnceleme with red header" (lines 152-174):**
- After plan 01: Birincil Öneri shows "Uzlaşma sağlanamadı" message, Fark Takibi shows "Tüm 4 model" message below
- Update: verify Birincil Öneri contains no-consensus message; verify Fark Takibi still shows "Tüm 4 model" and "ızgarada"

**Test: "shows manual form when Manuel Giriş button clicked in Tier 3" (lines 176-198):**
- Should still pass without changes (manual form unaffected)

**Test: "calls onAcceptSuggestion with GT triplets when Accept clicked in Tier 1" (lines 202-216):**
- Should still pass without changes (callback behavior unchanged)

**Test: "calls onAcceptSuggestion with majorityLabel when Çoğunluğu Kabul Et clicked in Tier 2" (lines 218-236):**
- Should still pass without changes

**Test: "calls onAcceptSuggestion with GT triplets when GT yi Koru clicked in Tier 2" (lines 238-255):**
- Should still pass without changes

**Test: "toggles manual form when Düzenle clicked" (lines 257-275):**
- Should still pass without changes

**Test: "calls onAddTriplet when manual form submitted in Tier 3" (lines 279-311):**
- Should still pass without changes

**Test: "shows original_llm_diff text in Tier 2 Diff Tracker" (lines 315-329):**
- After plan 02: diff text is text-sm (not text-[10px]), max-h-[140px] (not 60px)
- Update: verify diff text content is still present (text content assertion remains valid); no need to test CSS class values in unit tests

**Test: "shows manual triplets list with remove button" (lines 331-361):**
- Should still pass without changes

**New test: "Birincil Öneri shows majorityLabel triplets for Tier 1"**
- Render with Tier 1 props
- Assert: Birincil Öneri container textContent includes 'Manzara' and 'servis' (the majorityLabel terms)
- Assert: Birincil Öneri contains DiamondIcon SVG elements (one per triplet)

**New test: "Fark Takibi shows GT match confirmation for Tier 1"**
- Render with Tier 1 props
- Assert: Fark Takibi textContent includes "GT ile eşleşiyor"
- Assert: Fark Takibi does NOT contain the raw majorityLabel list (the triplets moved to Birincil Öneri)

**New test: "Touch targets are at least 44px"**
- Render with Tier 1, Tier 2, and Tier 3 props (3 renders)
- Query all button elements in the container
- Verify every button has min-height >= 44px via computed style or class check (min-h-[44px])

**New test: "Reduced-motion styles are present in rendered output"**
- Render component
- Assert: the rendered style element contains "@media (prefers-reduced-motion: no-preference)" text

**New test: "Zero emoji in rendered output"**
- Render component
- Assert: container.innerHTML does not match emoji Unicode range regex
  </action>
  <verify>
    <automated>cd frontend && npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    <item>ResolutionPanel.test.tsx: All 11 existing test cases updated where needed for new content ordering</item>
    <item>ResolutionPanel.test.tsx: New test verifies Birincil Öneri displays majorityLabel triplets for Tier 1</item>
    <item>ResolutionPanel.test.tsx: New test verifies Fark Takibi shows GT match confirmation (not triplet list) for Tier 1</item>
    <item>ResolutionPanel.test.tsx: New test verifies all buttons have min-height >= 44px (touch target compliance)</item>
    <item>ResolutionPanel.test.tsx: New test verifies reduced-motion @media block renders in output</item>
    <item>ResolutionPanel.test.tsx: New test verifies zero emoji (Unicode emoji range) in rendered HTML</item>
    <item>cd frontend && npm test exits 0 — all 88+ tests pass, 0 failures</item>
    <item>cd frontend && npx tsc --noEmit exits 0 — 0 TypeScript errors</item>
    <item>ResolutionPanel.tsx: grep for emoji returns 0 matches</item>
    <item>ResolutionPanel.test.tsx: grep for emoji returns 0 matches</item>
  </acceptance_criteria>
  <done>All tests pass with updated assertions; accessibility constraints verified; zero emoji confirmed</done>
</task>

<task type="auto">
  <name>Task 2: Final verification — full suite + accessibility + emoji scan</name>
  <files>frontend/src/components/ResolutionPanel.tsx, frontend/src/components/ResolutionPanel.test.tsx</files>
  <read_first>
Read the final state of both files after Task 1 modifications:
- frontend/src/components/ResolutionPanel.tsx — verify no emoji, verify reduced-motion, verify min-h-[44px]
- frontend/src/components/ResolutionPanel.test.tsx — verify all tests pass
  </read_first>
  <action>
Run comprehensive verification:

1. Run full test suite: `cd frontend && npm test`
2. Run TypeScript check: `cd frontend && npx tsc --noEmit`
3. Emoji scan: Scan both files for emoji characters using Unicode range grep
4. Verify reduced-motion: grep for `prefers-reduced-motion` in ResolutionPanel.tsx
5. Verify touch targets: grep for `min-h-\[44px\]` in ResolutionPanel.tsx (all action buttons)

If any step fails, fix the file and retest until all pass. Do NOT commit — orchestrator handles commits.
  </action>
  <verify>
    <automated>cd frontend && npm test 2>&1 | grep -E "(Tests|FAIL|PASS|Error)" | tail -10</automated>
  </verify>
  <acceptance_criteria>
    <item>cd frontend && npm test exits 0 — all tests pass (88+ tests, 0 failures)</item>
    <item>cd frontend && npx tsc --noEmit exits 0 — 0 TypeScript errors</item>
    <item>grep for emoji Unicode range in ResolutionPanel.tsx returns 0 matches</item>
    <item>grep for emoji Unicode range in ResolutionPanel.test.tsx returns 0 matches</item>
    <item>grep for "prefers-reduced-motion" in ResolutionPanel.tsx returns at least 1 match</item>
    <item>grep for "min-h-\[44px\]" in ResolutionPanel.tsx returns at least 4 matches (one per action button + Accept/Edit variant buttons)</item>
  </acceptance_criteria>
  <done>Full verification passes: all tests green, 0 TS errors, accessibility preserved, 0 emoji</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test→Component | Test creates DOM containers and renders components — no real backend data crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.6-05 | Tampering | ResolutionPanel.test.tsx | low | accept | Test assertions verify display behavior only; no data mutation path |
| T-07.6-06 | Information Disclosure | ResolutionPanel.test.tsx | low | accept | Test data uses hardcoded sample triplets — no real user data in tests |
| T-07.6-SC | Tampering | npm installs | high | mitigate | No new packages; vitest + existing deps only |
</threat_model>

<verification>
- `cd frontend && npm test` exits 0
- `cd frontend && npx tsc --noEmit` exits 0
- Emoji grep on both files returns 0 matches
- Reduced-motion @media block present in ResolutionPanel.tsx
- Touch targets verified: all buttons use min-h-[44px] or larger
</verification>

<success_criteria>
- All frontend tests pass (88+ tests, 0 failures)
- TypeScript compilation: 0 errors
- Zero emoji in both modified files
- Accessibility: reduced-motion respected, touch targets >= 44px
- Test coverage includes new layout assertions for Birincil Öneri and Fark Takibi
</success_criteria>

<output>
Create `.planning/phases/07.6-diff-readability/07.6-03-SUMMARY.md` when done
</output>
