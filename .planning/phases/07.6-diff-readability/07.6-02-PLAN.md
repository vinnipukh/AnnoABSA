---
phase: 07.6-diff-readability
plan: 02
type: execute
wave: 2
depends_on: [07.6-01]
files_modified:
  - frontend/src/components/ResolutionPanel.tsx
autonomous: true
requirements:
  - UX-02
must_haves:
  truths:
    - "The LLM diff text (originalLlmDiff) is displayed at a readable font size with adequate line spacing"
    - "The side-by-side Çoğunluk/GT comparison labels are large enough to distinguish triplet terms easily"
    - "The Fark Takibi (Diff Tracker) box provides generous vertical space so multi-line diffs are not truncated"
    - "The 4-way compare layout remains intact — no overlap with NLP toolbar, chatbox, or grid; the ResolutionPanel width stays within 300px"
  artifacts:
    - path: "frontend/src/components/ResolutionPanel.tsx"
      provides: "Readability-enhanced diff display with larger typography and spacing"
      contains: "originalLlmDiff"
      contains: "text-[11px]"
      contains: "max-h-[120px]"
  key_links:
    - from: "frontend/src/components/ResolutionPanel.tsx"
      to: "frontend/src/App.tsx"
      via: "Parent grid/flex layout — ResolutionPanel is a 280-300px fixed-width child; width change must not cause horizontal overflow or toolbar overlap"
      pattern: "w-\\[[23][0-9]{2}px\\]"
    - from: "frontend/src/components/ResolutionPanel.tsx"
      to: "frontend/src/components/FourWayGrid.tsx"
      via: "Sibling in parent flex container — grid flexes while panel is flex-shrink-0"
---

<objective>
Increase the visual size and spacing of diff content inside the ResolutionPanel so that reported differences (originalLlmDiff monospace text and the side-by-side Çoğunluk/GT comparison) are readable, while preserving the existing responsive 4-way layout and no-toolbar-overlap constraint.

Purpose: Currently the diff text uses `text-[10px]` (very small) with `max-h-[60px]` (only ~3-4 lines visible) and side-by-side labels at `text-[9px]`/`text-[10px]`. Zooming in is not an option because the parent layout is fixed-width. Enlarging the text and expanding the max-height makes differences visible without regressions.
Output: ResolutionPanel.tsx with larger font sizes, increased max-height on diff block, and slightly wider panel (280→300px max) with padding fine-tuning.
</objective>

<execution_context>
@$HOME/AppData/Local/hermes/gsd-core/workflows/execute-plan.md
@$HOME/AppData/Local/hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@frontend/src/components/ResolutionPanel.tsx
@frontend/src/components/FourWayGrid.tsx
@.planning/phases/07.6-diff-readability/07.6-01-PLAN.md

<interfaces>
ResolutionPanel.tsx current sizing to modify (exact line refs from CURRENT on-disk state after Plan 1 restructuring):

Panel wrapper (line ~172):
  `w-[280px]` — fixed width, flex-shrink-0, h-full

Fark Takibi header (line ~199):
  `text-[10px]` — "Fark Takibi" title

Diff monospace block (Tier 2, line ~225):
  `text-[10px] bg-base-300/50 p-2 rounded border border-base-300 font-mono text-base-content/70 leading-relaxed max-h-[60px] overflow-y-auto`

Side-by-side comparison headers (Tier 2, lines ~231, ~246):
  `text-[9px]` — "Çoğunluk" and "GT (Orijinal)" column headers

Side-by-side triplet entries (Tier 2, lines ~236, ~251):
  `text-[10px]` — individual triplet terms

Tier 1 majority label list (now in Birincil Öneri after Plan 1, line ~206):
  `text-[11px]` — triplet term text

Tier 1 empty state (line ~216):
  `text-[10px]` — "Uzlaşma üçlüsü yok"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enlarge diff display typography, spacing, and expand diff text area</name>
  <files>frontend/src/components/ResolutionPanel.tsx</files>
  <read_first>
Read the full file before modifying:
- frontend/src/components/ResolutionPanel.tsx — understand the current sizing classes on diff-related elements. Key lines to review: panel wrapper width near line 172, Fark Takibi header near line 199, diff monospace block near line 225, side-by-side comparison labels near lines 231/236/246/251.
- frontend/src/components/FourWayGrid.tsx — note that FourWayGrid is a sibling flex child; it uses `grid grid-cols-2 gap-3` with flexible column cards. Panel width change must not push grid cards below minimum readable width.
  </read_first>
  <action>
Adjust font sizes, spacing, and max-height of diff content within the ResolutionPanel's Fark Takibi (Diff Tracker) area. All changes are scoped to TEXT SIZE and SPACING only — no structural or content reordering (that was done in Plan 1).

**Panel width (conservative expansion):**
- Change the outer wrapper div from `w-[280px]` to `w-[300px]`. The 20px expansion adds breathing room for larger text without changing the flex-shrink-0 layout contract. The parent flex container divides space between FourWayGrid (flex-1) and ResolutionPanel (flex-shrink-0, now 300px).

**Fark Takibi header:**
- Change `text-[10px]` to `text-[11px]` on the "Fark Takibi" title div. Keep `font-bold`, `text-base-content/60`, `uppercase`, `tracking-wider`.

**LLM diff monospace block (Tier 2):**
- Change `text-[10px]` to `text-[11px]`
- Change `max-h-[60px]` to `max-h-[120px]` (doubles visible diff area, shows ~6-8 lines instead of ~3-4)
- Change `p-2` to `p-2.5` (slightly more padding around diff text)
- Keep: `bg-base-300/50 rounded border border-base-300 font-mono text-base-content/70 leading-relaxed overflow-y-auto`

**Side-by-side comparison headers (Tier 2, Çoğunluk and GT columns):**
- Change both `text-[9px]` to `text-[10px]` on the column header divs ("Çoğunluk" and "GT (Orijinal)")
- Keep: `font-bold uppercase`, respective color classes (`text-warning`, `text-primary`)

**Side-by-side triplet entries (Tier 2):**
- Change `text-[10px]` to `text-[11px]` on both the Çoğunluk and GT triplet term divs
- Keep: `truncate`, `flex items-center gap-1`, respective icons and colors

**Tier 1 diff tracker match confirmation:**
- For the "GT ile eşleşiyor" confirmation text (added by Plan 1 in Fark Takibi): use `text-[11px]` for the confirmation message text

**Tier 1 majority label list (in Birincil Öneri, from Plan 1):**
- Ensure triplet term text uses `text-[11px]` (larger than former `text-[10px]`)
- Ensure sentiment badges use `text-[10px]` (was `text-[10px]` — stable)

**Tier 3 message:**
- Change the "Tüm 4 model çıktısı..." message from `text-[11px]` to `text-xs` (12px — slightly larger for readability)
- Keep: `text-base-content/60 leading-relaxed`

**Empty state messages (across tiers):**
- Any italic empty-state text ("Uzlaşma üçlüsü yok", "—"): bump from `text-[10px]` to `text-[11px]`

**Action button area:**
- No changes — buttons already have `min-h-[44px]` and readable sizes.

**Constraints (hard rules):**
- Zero emoji — no icon changes, all Heroicons SVG remain
- Turkish UI copy untouched (string values unchanged)
- No prop interface changes
- DaisyUI + Tailwind only
- Reduced-motion `<style>` block preserved
- `getSentimentBadge` helper untouched
- No new dependencies or imports
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
    <automated>cd frontend && npx vitest run --reporter=verbose src/components/ResolutionPanel.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    <item>ResolutionPanel.tsx outer wrapper uses `w-[300px]` class (was `w-[280px]`)</item>
    <item>ResolutionPanel.tsx: diff monospace block has `text-[11px]` (was `text-[10px]`)</item>
    <item>ResolutionPanel.tsx: diff monospace block has `max-h-[120px]` (was `max-h-[60px]`)</item>
    <item>ResolutionPanel.tsx: diff monospace block has `p-2.5` (was `p-2`)</item>
    <item>ResolutionPanel.tsx: side-by-side Çoğunluk/GT column headers are `text-[10px]` (were `text-[9px]`)</item>
    <item>ResolutionPanel.tsx: side-by-side triplet entries are `text-[11px]` (were `text-[10px]`)</item>
    <item>ResolutionPanel.tsx: Fark Takibi title is `text-[11px]` (was `text-[10px]`)</item>
    <item>ResolutionPanel.tsx: Tier 3 message is `text-xs` (was `text-[11px]`)</item>
    <item>ResolutionPanel.tsx: No emoji in any rendered or string content</item>
    <item>`cd frontend && npx tsc --noEmit` exits 0</item>
    <item>All existing ResolutionPanel tests pass (failures may indicate test needs updating — that is handled in Plan 3)</item>
  </acceptance_criteria>
  <done>Diff content in ResolutionPanel uses larger typography (11px minimum in diff areas), diff text area expanded to 120px max-height, and panel widened to 300px without breaking the 4-way layout.</done>
</task>

</tasks>

<artifacts>
### Artifacts this plan modifies

| Artifact | Kind | Path | Description |
|----------|------|------|-------------|
| `ResolutionPanel` wrapper width | modified | `frontend/src/components/ResolutionPanel.tsx` | `w-[280px]` → `w-[300px]` |
| Diff monospace block sizing | modified | `frontend/src/components/ResolutionPanel.tsx` | Font: `text-[10px]`→`text-[11px]`, max-h: `60px`→`120px`, padding: `p-2`→`p-2.5` |
| Side-by-side column headers | modified | `frontend/src/components/ResolutionPanel.tsx` | `text-[9px]`→`text-[10px]` for "Çoğunluk" and "GT (Orijinal)" |
| Side-by-side triplet entries | modified | `frontend/src/components/ResolutionPanel.tsx` | `text-[10px]`→`text-[11px]` for individual triplet terms |
| Fark Takibi header | modified | `frontend/src/components/ResolutionPanel.tsx` | `text-[10px]`→`text-[11px]` |
| Tier 3 message text | modified | `frontend/src/components/ResolutionPanel.tsx` | `text-[11px]`→`text-xs` (12px) |
| Empty state messages | modified | `frontend/src/components/ResolutionPanel.tsx` | `text-[10px]`→`text-[11px]` |
</artifacts>

<ui_ux_constraints>
### UI/UX Constraints

The following rules apply to ALL changes in this plan. Failure to follow any of them is a hard gate:

1. **No emoji icons** — all icons MUST be Heroicons inline SVG. No `✅`, `❌`, `⚠️`, `💡`, or any Unicode emoji in rendered content, button labels, toast messages, comments, or string literals. Scan the ENTIRE modified file for residual emoji from pre-existing code (`setSaveToast`, button text, labels), not just new lines.

2. **Touch targets ≥ 44×44px** — all clickable elements (buttons, toggles) must have `min-h-[44px]`. This plan does not add new buttons; existing buttons already comply.

3. **Color is never the sole indicator** — all diff sizing changes are text/spacing only; no color changes. Existing tier color coding (success/warning/error) is paired with icons and text labels.

4. **Reduced-motion respected** — the existing `<style>` block with `@media (prefers-reduced-motion: no-preference)` must be preserved verbatim. No new transitions or animations introduced.

5. **Aria-labels on icon-only buttons** — no new icon-only buttons introduced. Existing aria-labels preserved.

6. **Button style consistency** — no button style changes in this plan. Existing inline Tailwind pattern preserved.

7. **Turkish UI copy** — all text strings unchanged (Turkish diacritics preserved). Only CSS class values change.

8. **Async null-safety** — no async data access patterns changed. Props continue to arrive already loaded.

9. **No layout breakage** — panel width increase from 280px to 300px must not cause horizontal overflow or overlap with NLP toolbar/chat. Test by checking parent flex container: FourWayGrid + ResolutionPanel should occupy the available space without scrollbar at 1280px viewport minimum.

10. **Modified-file emoji scanning** — after all changes, run `grep -rnP '[\x{1F300}-\x{1F9FF}\x{2600}-\x{27BF}]' frontend/src/components/ResolutionPanel.tsx`. Must return 0 matches.
</ui_ux_constraints>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parent→ResolutionPanel | Props unchanged — no new data flow, no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.6-03 | Denial of Service | ResolutionPanel.tsx | low | accept | CSS-only changes; no runtime logic modified. Layout width increase is 20px — cannot cause rendering DoS. |
| T-07.6-SC | Tampering | npm installs | high | mitigate | No new packages introduced; only existing deps used |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0 — no TypeScript errors from class name changes
- `cd frontend && npm test` exits 0 — all existing tests still pass (test assertions may need Plan 3 updates for exact text sizes)
- Visual inspection: diff text in Tier 2 Fark Takibi is visibly larger; diff block shows more lines before scrolling
- `grep -rnP '[\x{1F300}-\x{1F9FF}\x{2600}-\x{27BF}]' frontend/src/components/ResolutionPanel.tsx` returns 0 — zero emoji
- Manual check: at 1280px viewport width, the 4-way grid + 300px resolution panel fit without horizontal scrollbar
</verification>

<success_criteria>
- Diff monospace text rendered at 11px (was 10px) with 120px max-height (was 60px) for readable multi-line LLM diffs
- Side-by-side comparison labels readable at 10px headers / 11px entries (were 9px / 10px)
- Panel width at 300px does not break 4-way grid layout at 1280px minimum viewport
- Zero emoji, zero TypeScript errors, zero new dependencies
</success_criteria>

<output>
Create `.planning/phases/07.6-diff-readability/07.6-02-SUMMARY.md` when done
</output>
