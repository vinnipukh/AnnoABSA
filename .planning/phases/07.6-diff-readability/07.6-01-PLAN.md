---
phase: 07.6-diff-readability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ResolutionPanel.tsx
autonomous: true
requirements:
  - UX-01
must_haves:
  truths:
    - "The Birincil Öneri box displays actual LLM-suggested triplet labels, not just advisory text"
    - "The LLM diff text (originalLlmDiff) appears below the suggested label section in the Fark Takibi area"
    - "The majority label triplets (Çoğunluk) appear as the LLM-suggested labels, with diff/comparison content beneath them"
    - "All three tiers (1/2/3) maintain their distinct visual treatment while following the new content ordering"
  artifacts:
    - path: "frontend/src/components/ResolutionPanel.tsx"
      provides: "Restructured resolution panel with LLM-suggested labels above diff content"
      contains: "Birincil Öneri"
      contains: "Fark Takibi"
      contains: "majorityLabel"
      contains: "originalLlmDiff"
  key_links:
    - from: "frontend/src/components/ResolutionPanel.tsx"
      to: "frontend/src/App.tsx"
      via: "ResolutionPanel props (majorityLabel, originalLlmDiff) — no prop interface changes"
      pattern: "originalLlmDiff"
    - from: "frontend/src/components/ResolutionPanel.tsx"
      to: "frontend/src/types.ts"
      via: "TripletItem type used for majorityLabel display"
      pattern: "TripletItem.*from.*types"
---

<objective>
Restructure the ResolutionPanel so that LLM-suggested labels (majority_label triplets) display prominently in the "Birincil Öneri" (Primary Suggestion) area, and the LLM diff text and majority label comparison content appear below them in the "Fark Takibi" (Diff Tracker) area.

Purpose: Users currently see advisory text in Birincil Öneri but the actual LLM-suggested triplet labels are buried below in Fark Takibi. Moving the labels up and the diff text down creates a logical top-to-bottom flow: "here is what the LLMs suggest" → "here is how it differs from GT."
Output: Restructured ResolutionPanel.tsx with reordered content sections for all three tiers.
</objective>

<execution_context>
@$HOME/AppData/Local/hermes/gsd-core/workflows/execute-plan.md
@$HOME/AppData/Local/hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@frontend/src/components/ResolutionPanel.tsx
@frontend/src/types.ts

<interfaces>
Relevant types from frontend/src/types.ts:
```typescript
export interface TripletItem {
  id: string;
  aspect_term: string;
  aspect_category: string;
  sentiment_polarity: string;
  isSelected?: boolean;
}
```

ResolutionPanelProps (from ResolutionPanel.tsx lines 4-17):
- majorityVote: number
- majorityLabel: TripletItem[]
- gtTriplets: TripletItem[]
- consensusIntersection: TripletItem[]
- originalLlmDiff: string
- categories: string[]
- polarities: string[]
- manualTriplets: TripletItem[]
- onAddTriplet, onRemoveTriplet, onAcceptSuggestion, onEditTriplets
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure ResolutionPanel — move LLM-suggested labels into Birincil Öneri and diff content below</name>
  <files>frontend/src/components/ResolutionPanel.tsx</files>
  <read_first>
Read the full file before modifying:
- frontend/src/components/ResolutionPanel.tsx — understand Tier 1/2/3 conditional rendering inside "Birincil Öneri" (lines 184-195) and "Fark Takibi" (lines 198-270)
- frontend/src/types.ts — TripletItem shape
  </read_first>
  <action>
Restructure ResolutionPanel.tsx to reorder content so LLM-suggested labels appear in Birincil Öneri and diff content follows below in Fark Takibi.

**Tier 1 (Otomatik Kabul) changes:**
- In "Birincil Öneri" box (lines 184-195): Replace the text-only suggestion with a visual display of majorityLabel triplets. Show each triplet as: DiamondIcon SVG + `"{aspect_term}"` + sentiment badge (using getSentimentBadge helper). Keep the tier suggestion text as a subtitle/caption above or below the labels. The box header text remains "Birincil Öneri".
- In "Fark Takibi" box (lines 198-270): For Tier 1, replace the majority label list (currently lines 204-219) with a compact match confirmation line: a green-bordered row showing "GT ile eşleşiyor" text + CheckCircleIcon. Remove the former majority label list from Fark Takibi entirely — it now lives in Birincil Öneri.

**Tier 2 (Hızlı Fark) changes:**
- In "Birincil Öneri" box: Replace the text-only suggestion with a display of majorityLabel triplets as Çoğunluk suggestion labels. Show each triplet identically to Tier 1 (DiamondIcon + term + badge). Subtitle: the suggestion text about verifying the difference.
- In "Fark Takibi" box: Reorder internal content so the LLM diff text (originalLlmDiff monospace block) appears BELOW the side-by-side Çoğunluk vs GT comparison. Currently diff text is ABOVE the comparison (lines 224-228 before 229-260). Swap: the 2-column comparison grid comes first, then the diff monospace block below it. Add a thin separator (border-t border-base-300) between comparison and diff text. The LLM diff text is now truly "under" the suggested labels.

**Tier 3 (Manuel İnceleme) changes:**
- In "Birincil Öneri" box: Show a message that no consensus triplet exists (XCircleIcon + "Uzlaşma sağlanamadı — tüm modelleri inceleyin").
- In "Fark Takibi" box: Keep the existing "Tüm 4 model çıktısı yukarıdaki ızgarada..." message, but move it to appear after/under the suggestion. The suggestion and the instruction form a top-to-bottom reading order.

**Constraints (hard rules — do not break):**
- Zero emoji — all icons remain Heroicons SVG inline
- Turkish UI copy preserved (Birincil Öneri, Fark Takibi, Çoğunluk, etc.)
- No prop interface changes to ResolutionPanelProps
- DaisyUI + Tailwind only — no new CSS files
- Touch targets stay min-h-[44px] on all clickable elements
- Reduced-motion @media block preserved (lines 160-169)
- Existing helper functions (getSentimentBadge, useMemo for tier/gtMatchesMajority) reused, not rewritten
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=verbose src/components/ResolutionPanel.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    <item>ResolutionPanel.tsx: "Birincil Öneri" box renders majorityLabel triplets for Tier 1 with DiamondIcon + term + sentiment badge</item>
    <item>ResolutionPanel.tsx: "Birincil Öneri" box renders majorityLabel triplets for Tier 2 with DiamondIcon + term + sentiment badge</item>
    <item>ResolutionPanel.tsx: "Birincil Öneri" box renders XCircleIcon + Turkish no-consensus message for Tier 3</item>
    <item>ResolutionPanel.tsx: Tier 1 "Fark Takibi" contains "GT ile eşleşiyor" confirmation line, NOT the former majorityLabel list</item>
    <item>ResolutionPanel.tsx: Tier 2 "Fark Takibi" has the 2-column Çoğunluk/GT comparison grid BEFORE the originalLlmDiff monospace block (diff is below/under)</item>
    <item>ResolutionPanel.tsx: No emoji in any rendered or string content</item>
    <item>ResolutionPanel.tsx: All Heroicons SVG imports still used (no font icons)</item>
    <item>ResolutionPanel.test.tsx: All existing tests pass (may need test assertion updates in plan 3)</item>
  </acceptance_criteria>
  <done>ResolutionPanel renders with LLM-suggested labels in Birincil Öneri and diff content below in Fark Takibi for all 3 tiers</done>
</task>

</tasks>

<artifacts>
### Artifacts this phase produces

| Artifact | Kind | Path | Description |
|----------|------|------|-------------|
| `ResolutionPanel` component | modified | `frontend/src/components/ResolutionPanel.tsx` | Restructured: Birincil Öneri now renders majorityLabel triplets with DiamondIcon + sentiment badge; Fark Takibi content reordered |
| `tierConfig.suggestion` string | reused | `frontend/src/components/ResolutionPanel.tsx` | Suggestion strings for Tiers 1-3 repositioned as subtitle/caption — string values UNCHANGED (exact-text test assertions must keep passing; sizing changes are scoped to Plan 2) |
| `getSentimentBadge` helper | reused | `frontend/src/components/ResolutionPanel.tsx` | No changes — reused for sentiment badges in Birincil Öneri triplet display |
</artifacts>

<ui_ux_constraints>
### UI/UX Constraints

The following rules apply to ALL changes in this plan. Failure to follow any of them is a hard gate:

1. **No emoji icons** — all icons MUST be Heroicons inline SVG. No `✅`, `❌`, `⚠️`, `💡`, or any Unicode emoji in rendered content, button labels, toast messages, comments, or string literals. Scan the ENTIRE modified file for residual emoji from pre-existing code (`setSaveToast`, button text, labels), not just new lines.

2. **Touch targets ≥ 44×44px** — all clickable elements (buttons, toggles) must have `min-h-[44px]`. Icon-only buttons must include `aria-label` attributes (e.g., `aria-label="Kaydet"`, `aria-label="Kapat"`).

3. **Color is never the sole indicator** — always pair color with text, icons, or pattern. Tier badges use color (`bg-success/10`) + icon (`CheckCircleIcon`) + text label (`Otomatik Kabul`).

4. **Reduced-motion respected** — any CSS transition/animation MUST be wrapped in `@media (prefers-reduced-motion: no-preference)`. The existing `<style>` block at lines 160-169 of ResolutionPanel.tsx already does this; preserve that block verbatim unless adding new transition classes.

5. **Aria-labels on icon-only buttons** — every button containing only an SVG icon must have an explicit `aria-label` (Turkish for feature UI, e.g., `"Üçlüyü kaldır"`). Existing buttons already follow this; new icon buttons must too.

6. **Button style consistency** — match existing toolbar/button patterns. DaisyUI `btn` classes and inline Tailwind must NOT be mixed on the same visible element. ResolutionPanel currently uses inline Tailwind (`bg-success`, `rounded-lg`, etc.) consistently — do NOT introduce DaisyUI `btn` class on panel buttons.

7. **Turkish UI copy** — main feature text remains Turkish with correct diacritics: `Birincil Öneri`, `Fark Takibi`, `Çoğunluk`, `GT (Orijinal)`, `Otomatik Kabul`, `Hızlı Fark`, `Manuel İnceleme`.

8. **Async null-safety** — all render-time accesses to async-loaded data use optional chaining: `data?.field`. Props from parent (majorityLabel, gtTriplets) arrive already loaded; no optional chain needed.

9. **Modified-file emoji scanning** — after all changes, run `grep -rnP '[\x{1F300}-\x{1F9FF}\x{2600}-\x{27BF}]'` on the modified file. Must return 0 matches.
</ui_ux_constraints>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parent→ResolutionPanel | Props (majorityLabel, originalLlmDiff) are data from backend — rendered as trusted display content, no user input at this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.6-01 | Tampering | ResolutionPanel.tsx | low | accept | Props are read-only display data from backend; no mutation path exists |
| T-07.6-02 | Information Disclosure | ResolutionPanel.tsx | low | accept | All rendered data already visible in current layout; repositioning does not expose new information |
| T-07.6-SC | Tampering | npm installs | high | mitigate | No new packages introduced; only existing deps used |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0 — no TypeScript errors
- `cd frontend && npm test` exits 0 — all existing tests still pass (test assertions may need updating in plan 3)
- Visual inspection: Birincil Öneri shows triplet labels, not just text; Fark Takibi comparison is above diff text
- `grep -rnP '[\x{1F300}-\x{1F9FF}\x{2600}-\x{27BF}]' frontend/src/components/ResolutionPanel.tsx` returns 0 — zero emoji
</verification>

<success_criteria>
- ResolutionPanel renders with the suggested triplet labels visible in Birincil Öneri for all 3 tiers
- The LLM diff text and majority label comparison content appear below/after the suggestion labels
- No regression in existing test pass count; no new TypeScript errors; no emoji in source
</success_criteria>

<output>
Create `.planning/phases/07.6-diff-readability/07.6-01-SUMMARY.md` when done
</output>
